---
title: ADD
description: Sumar dos valores de memoria
layout: ../../../layouts/MainLayout.astro
---

import Code from "../../../components/Code/Code";
import Solution from "../../../components/Solution";
import states from "./states.json";

Sumar dos valores de memoria
## Sintaxis

<Code
  code={`MOV <SRC>, <DST>`}
  testCodeText="Probar código"
  previewText="Vista previa"
  client:load
/>

pudiendo ser:
- `SRC` una variable o una dirección de memoria (0x00 - 0x7F)
- `DST` una variable o una dirección de memoria (0x00 - 0x7F)

El resultado de la operación se almacena en la dirección de memoria de `DST`.

### Sumar dos valores de memoria

Dada la siguiente sección de datos:

<Code
  code={`.data
dw		num1 10
dw		num2 15`}
  testCodeText="Probar código"
  previewText="Vista previa"
  client:load
/>
Procederemos a sumar los valores de las variables `num1` y `num2` y almacenar el resultado en la variable 

<Code
  code={`.ver 1

.data
dw		num1 10
dw		num2 15

.fin	lblend

.code
add num1, num2

lblend:`}
  testCodeText="Probar código"
  previewText="Vista previa"
  client:load
/>

#### Microprograma

| ESTADO | MICRO-OPERACIÓN |
| :----- | :-------------- |
| S0     | {states.S0}     |
| S1     | {states.S1}     |
| S2     | {states.S2}     |
| S6     | {states.S6}     |
| S7     | {states.S7}     |

Lo mismo sería si en lugar de utilizar variables, utilizamos direcciones de memoria:

<Code
  code={`.ver 1

.data
dw		num1 10
dw		num2 15

.fin	lblend

.code
add 0x2, 0x3

lblend:`}
  testCodeText="Probar código"
  previewText="Vista previa"
  client:load
/>

## Comprueba tus conocimientos

Crea un programa que haga la siguiente operación: `RESULTADO = 0x5 + 0x2`

**Nota**: debes conocer y utilizar (o no) la operación: [`MOV`](/es/instructions/MOV)

<Solution
  title="Solución"
  sol={[`.ver 1

.data
dw		num1 0x5
dw		num2 0x2
dw		resultado 0

.fin	lblend

.code
add num1, num2
mov num2, resultado

lblend:`, `.ver 1

.data
dw		num1 0x5
dw		resultado 0x2

.fin	lblend

.code
add num1, resultado

lblend:`]}
  testCodeText="Probar código"
  client:load
/>


